%h1 Caronas Disponíveis

%table
  %tr
    %th Hora ida
    %th Hora volta
    %th Limite pessoas
    %th Valor por pessoa
    

  - @caronas.each do |carona|
    %tr
      %td= carona.hora_ida.strftime('%H:%M:%S')
      %td= carona.hora_volta.strftime('%H:%M:%S')
      %td= carona.limite_pessoas
      %td= carona.valor_por_pessoa
      %td= link_to 'Checar', carona
      </br>
      -if carona.owner == current_user.id
        %td= link_to 'Editar', edit_carona_path(carona)
        %td= link_to 'Deletar', carona, :method => :delete, :data => { :confirm => 'Tem certeza?' }
      %td  
        %button{:class => "btn"}= link_to "getCarona o/", adicionar_caroneiro_path(carona)

        
%br

= link_to 'Nova Carona', new_carona_path
